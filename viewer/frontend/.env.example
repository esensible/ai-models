# Populated automatically by `make deploy` from Pulumi outputs.
# For local `npm run dev`, copy to .env and fill in:
VITE_WS_URL=wss://xxxx.execute-api.ap-southeast-2.amazonaws.com/prod
VITE_API_TOKEN=your-shared-token
